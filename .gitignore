node_modules
.env
.env.local
dist
*.log
.cache
.gmat-docs-mcp
~/.gmat-docs-mcp
*.tmp
*.temp
.DS_Store

# Keep embeddings.json in the repository for easy deployment
# cache/

# Local embedding model cache (downloaded on first run) and embedding backups
data/models/
*.bak.json

# pnpm is the canonical package manager (pnpm-lock.yaml)
package-lock.json
# Skill eval workspaces and harvested community scripts (mixed licenses - do not redistribute).
# The harvest manifest (pointers to public repos) and INDEX are publishable; the scripts are not.
skills/*-workspace/
data/community-scripts/*
!data/community-scripts/manifest.example.json
